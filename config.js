var config = {
    database:{
        host:   'localhost',
        user:   'root',
        password:   'root',
        port:   3306,
        db: 'bc'
    }, 
    server: {
        host: '127.0.0.1',
        port: '8000'
    }
}

module.exports = config